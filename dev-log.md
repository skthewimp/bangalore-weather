# Dev Log

Auto-generated by Claude Code SessionEnd hook.

## Session — 2026-04-30 — Generalize chart-subtitle commentary

### Problem

Today's daily-snapshot chart (Apr 30) reported the rainfall window as "trace rainfall only" - missing the actual story. The 14-day window had ~30 dry days preceding it then a single big spike on Apr 29. Aggregated total looked tiny; the drought-break was the headline.

Root cause: `bangalore_weather_update.R` was sending the LLM only window-aggregate stats (avg high, avg low, total rain, rainy days, hottest, coldest, records). No daily distribution, no streak signals. Model literally could not see the pattern.

### Fix attempt 1 (overfit)

Added pre-computed `wettest_day`, `spike_share`, `dry_streak_preceding_first_rain`, `daily_rain_seq`, `temp_swings >=4°C`. Updated system prompt to call out concentration + drought-break specifically.

Caught immediately as overfit: this only finds yesterday's pattern. If next month's story is back-to-back wet days, a heat dome, a sustained warm-night anomaly, a cool snap mid-window — those metrics either miss it or actively mislead.

### Fix attempt 2 (general, R does the heavy lifting)

Refactored `bangalore_weather_update.R` lines ~150-280:

R now computes a wide battery of candidate signals every run:
- Per-day actual + climatological normal + deviation table (high, low, rain) - shown to LLM as text
- Wettest 2/3/5-day rolling stretches with dates
- Longest run-length streaks within window for: dry, wet, hot-day (high ≥2°C above normal), cool-day, warm-night (low ≥2°C above normal), cool-night - via a generic `longest_run` + `rle()` helper
- Antecedent dry/wet streaks extending before the window
- Records broken since 1981 (kept from before)
- Window summary numbers vs normal

System prompt rewritten to drop pattern-specific framing. Now: "Numbers are pre-computed; pick the most striking signals from this list. Could be multi-day rain, drought-break, heat streak, record day, sustained warm-night anomaly, etc. Don't default to any one frame. Don't blur a real signal into a window average."

Heavy lifting stays in R. LLM = curator + writer.

Removed dead vars `recent_cal`, `normal_temp`, `normal_rain` made unused by the refactor.

## Session 367510dd — 2026-04-30T07:04:26 (reason: clear)

### Prompts

- there is this theory that in summer (April / May), showers can cool down bangalore signficnatly. is tehre anything to this? does it vary by time of months? can you analyse this?

### Assistant actions

- Plan: pull Apr/May hourly temp+rain, measure temp drop around rain events, see variation by part-of-month. State approach before code.

Approach:
- Rain event = hour with precip > threshold (e.g. >0.5mm)
- For each event, compute temp before (e.g. avg t-3h to t-1h) vs after (t+1h to t+3h), and the min temp after
- Compare to non-rain Apr/May days as baseline diurnal cooling
- Bin by half-month (early Apr / late Apr / early May / late May) to see variation

OK with this? Or want different cut (e.g. only afternoon thunderstorms 3pm-9pm, since "summer showers" usually means convective evening rai…
- Data: hourly 1981-2026, temp in °C, rain in mm. Proceeding with analysis.
- Strong signal. Now chart.
- ## Yes, theory hold up. Numbers:

**Per-shower effect** (event-based, 718 afternoon showers Apr-May 1981-2026):
- Avg temp drop within 5 hrs of shower onset: **6.4°C** (median 6.65, 90th pctile 8.4)
- Pre-rain temp 29.9°C → post-rain min 23.5°C

**Vs natural diurnal cooling** (rainy vs dry afternoons):
- Dry afternoon: peak 33.2°C → 8 PM evening 26.0°C (drop 7.3°C)
- Rainy afternoon: peak 31.4°C → evening 23.4°C (drop 8.0°C)
- Rain shaves ~1.8°C off peak AND ~2.6°C off evening. Net "extra cooling" beyond natural drop = ~0.7°C, but compounded effect on perceived heat much bigger.

**By half-mon…

